# 変更履歴

このプロジェクトの主な変更点を記録します。

書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、
バージョン番号は [セマンティック バージョニング](https://semver.org/lang/ja/) に従います。

## [0.5.1] - 2026-09-05

バージョン 6 と 8 が動作するようになりました。テストセットの 34 本すべてがプレイできます。

### 修正

* **バージョン 8 の作品が動作しませんでした。** `pull` が結果を格納するのは
  バージョン 6 のみですが、判定が `<= 5` だったためバージョン 7・8 がバージョン 6 の
  経路を通っていました。存在しない store バイトを消費して命令境界がずれ、さらに
  スタックを 1 回余分に pop していました。`advent.z8` は「Stack underflow」で
  停止していました。
* **バージョン 6 の作品が誤ったアドレスから開始していました。** バージョン 6 では
  $06 のワードは `main` ルーチンの**パックドアドレス**で、呼び出す必要があります。
  従来はバイトアドレスとみなしてジャンプしていました。`zork0.z6` は最初の命令で
  破綻していました。
* **バージョン 6・7 のルーチン／文字列オフセットが無視されていました。** パックド
  アドレスに $28・$2A のオフセットを加算するようにしました。
* **`throw` が未実装**でエラーを投げていました。`catch` が返した深さまで呼び出し
  スタックを巻き戻し、そのルーチンから復帰するようにしました。
* **Z-machine の出力バッファが際限なく成長していました。** 作品が出力した文字を
  すべて溜め込む一方、読み出す処理は無く、解放は `reset-story` のみでした。出力量が
  多い作品でヒープを使い切ります。`*output-buffer-limit*`（64K）で打ち切るように
  しました。
* **`read_char` が入力終端でエラーになっていました。** 改行を返すようにしました。
* バージョン 6 はカーソル位置を文字単位ではなくピクセル単位で指定するため、テキスト
  画面には収まらない行番号を要求されることがあります。捕捉する上部ウィンドウの行数に
  上限を設けました。

### 追加

* **バージョン 6 の拡張オペコードをスタブとして実装**: `draw_picture`
  `picture_data` `erase_picture` `set_margins` `set_true_colour` `move_window`
  `window_size` `window_style` `get_wind_prop` `scroll_window` `pop_stack`
  `read_mouse` `mouse_window` `push_stack` `put_wind_prop` `print_form`
  `make_menu` `picture_table` `buffer_screen`。本インタプリタにはグラフィックも
  実ウィンドウもないため、重要なのは**各命令が標準どおりの store バイト・分岐バイトを
  正しく消費すること**です。これを飛ばすと命令境界が失われます。store / branch の
  区分は Z-Machine Standards Document 1.1 の第14章のオペコード表から取りました。

### 検証

| バージョン | 本数 | 結果 |
|:--|--:|:--|
| 3 | 17 | 全て動作 |
| 4 | 3 | 全て動作 |
| 5 | 8 | 全て動作 |
| 6 | 2 | テキストとして動作 |
| 8 | 1 | 動作 |

### 既知の制限

* バージョン 6 のグラフィックとピクセル単位のウィンドウ配置は再現していません。
  作品は動作し可読ですが、画像は表示されず、上部ウィンドウはピクセル配置ではなく
  ステータスバーとして描画されます。
* バージョン 4 のタイマー付き入力（`read` の追加オペランド）は引き続き無視されます。

## [0.5.0] - 2026-09-05

バージョン 5 の作品が動作するようになりました。34 本のストーリーファイルで検証し、
テストセット中のバージョン 3 とバージョン 5 の作品はすべてプレイできます。

### 修正

* **拡張オペコードのディスパッチ先が誤っており、バージョン 5 の作品が一切動作しません
  でした。** `0xBE` プレフィックスのデコード自体は正しかったのですが、その後 VAR
  オペコードのテーブルを引いていたため、`EXT:9`（`save_undo`）が `VAR:9`（`pull`）
  として実行され、空のスタックを pop していました。`Advent.z5` は最初のコマンドで
  「Stack underflow」で停止していました。拡張オペコードのテーブルは一度も参照されて
  いませんでした。

### 追加

* **バージョン 5 の拡張オペコード**: `save_table` `restore_table` `log_shift`
  `art_shift` `set_font` `save_undo` `restore_undo` `print_unicode`
  `check_unicode`。バージョン 6 専用の画像関連命令は対象外としています。
* **上部ウィンドウ** — `split_window` `set_window` `set_cursor` `get_cursor`
  `erase_window` が空実装だったため、作品が描くステータス行が本文に混ざって
  いました。上部ウィンドウへの書き込みを捕捉し、メインウィンドウに戻った時点で
  ステータスバーとして描画します。
* **インタプリタの能力をヘッダに宣言するようにしました。** 従来はストーリーを読み込む
  だけで、インタプリタ側の情報を一切書き込んでいませんでした。そのため作品側が
  ステータス行のレイアウトを決められず、使える文字装飾も判断できませんでした。
  Flags1・Flags2、画面サイズ、インタプリタ番号、標準リビジョンを実行開始前に書き込み、
  リスタート時にも再適用します。
  * `*screen-columns*`（80）と `*screen-rows*`（24）で画面サイズを指定します
  * `*interpreter-number*`（6 = IBM PC）でインタプリタの種別を指定します
* `set_text_style` を ANSI にマッピングしました（反転・太字・斜体）。
* **未実装の命令に当たってもストーリーが終了しなくなりました。** 一度だけ報告して
  読み飛ばします。`*strict-opcodes*` を `T` にすると従来どおり停止します。デコードの
  問題を追う際に有用です。

### 既知の制限

* バージョン 6 はグラフィックと複数ウィンドウが前提で、`advent.z6` と `zork0.z6` は
  動作しません。
* バージョン 8 はまだ動作しません。`advent.z8` はルーチン内で命令境界がずれます。
  原因は未特定です。
* バージョン 4 のタイマー付き入力（`read` の追加オペランド）は無視されます。

## [0.4.1] - 2026-09-05

### 追加

* **3 種類の出力を表示で区別するようにしました。** 原文（英語）を減光、訳文を
  明るい白、ステータス行を青地に明るい白にし、一目で見分けられるようにしました。
  * `*ansi-enabled*` の既定は `T` です。端末以外への出力でエスケープを落としたい
    場合は `:auto`、色付けをやめる場合は `NIL` を指定します
  * `*ansi-source*` `*ansi-translation*` `*ansi-status*` に SGR パラメータを
    直接指定できるので、配色は自由に変更できます
  * エスケープシーケンスは端末にのみ書き出し、Z-machine の出力バッファには
    入れません。プロンプトで既定の表示に戻すため、入力した文字には色が付きません
* **ステータス行**（V1-3）。`show_status` が空実装だったため、現在地もスコアも
  画面に出ていませんでした。現在地・スコア・手数を、`>` プロンプトの直前に 1 行で
  表示するようにしました。
  * バイリンガルモードでは現在地を翻訳し、`家の西側 (West of House)` の形で表示します
  * time game のストーリーでは、スコアと手数の代わりに `Time: hh:mm` を表示します
  * 本インタプリタには画面モデルが無いため、画面最上部に固定するのではなく、
    本文と一緒にスクロールする 1 行として出力します
  * `*status-line-enabled*` で無効化、`*status-line-width*` で右寄せの桁位置を
    指定できます（CJK 文字は 2 桁として計算します）

### 変更

* **部分一致を最後の手段にし、既定で無効にしました。** 完全一致するキャッシュが
  無いとき、従来は「行の 1/3 以上を占めるキャッシュ済みの英文」の訳をそのまま
  返していました。`Forest` が `Forest Path` の訳として使われてしまう、といった
  誤りが起きます。さらに悪いことに、この処理は翻訳 API **より先**に走り、結果を
  キャッシュに保存しなかったため、一度該当した行は何度現れても正しく翻訳されません
  でした。
  * 順序を「完全一致 → 大文字小文字無視 → API → 部分一致」に変更しました
  * `*use-partial-matches*` の既定は `NIL` です。翻訳バックエンド無しで遊ぶ場合は
    `T` にすると従来の代替動作が有効になります
  * `*partial-match-threshold*`（既定 `0.7`）で、キャッシュ済みの語句が行全体の
    何割を占める必要があるかを指定します。固定値の 1/3 を置き換えました
  * 同梱の日本語キャッシュで検証したところ、旧規則では 179 件中 12 件で誤った断片が
    採用されていました。新しい閾値では 4 件に減り、順序変更によりバックエンドを
    設定していれば画面には一切出ません

### 削除

* 訳文が文として未完だった同梱エントリ 2 件、`"This gives you the rank of"` と
  `"Your score is"` を削除しました。どちらも助詞で終わっており、途中で切れた出力に
  しかなりません。完全な文は API が正しく翻訳し、キャッシュされます。`brass lantern`
  や `West of House` のように単独で表示される物品名・部屋名はもちろん残しています。

## [0.4.0] - 2026-09-05

Ollama によるローカル LLM 翻訳と、プレイ全体を通して訳語を統一する用語集を追加しました。

### 追加

* **Ollama バックエンド**（`ollama.lisp`）— ローカルにインストール済みの任意のモデルで
  翻訳できます。API キーは不要です。
  * `(setup-ollama "qwen3.5:9b")` でモデルを指定。サーバの URL も指定できます:
    `(setup-ollama "gemma3:12b" "http://192.168.1.10:11434")`
  * `(list-ollama-models)` でインストール済みモデルの一覧、`(set-ollama-model ...)` で
    切り替え、`(test-ollama)` で接続確認
  * `setup-ollama` はサーバに到達できない場合やモデルが未導入の場合に警告し、
    必要な `ollama pull` コマンドを表示します
  * 調整用変数: `*ollama-url*` `*ollama-model*` `*ollama-temperature*`
    `*ollama-num-predict*` `*ollama-think*`
* **用語集**（`glossary.lisp`）— 固有名詞やゲーム用語の訳語を固定し、同じ英語表現が
  常に同じ訳語になるようにします。
  * 原文に実際に含まれる用語だけをプロンプトに差し込むため、プロンプトが肥大しません
  * 訳語が落ちた場合、その語を強調して 1 回だけ再翻訳します（`*glossary-enforce*`）
  * `(show-glossary)` `(add-glossary ...)` `(remove-glossary ...)` `(save-glossary)`
  * `(glossary-check)` でキャッシュ済み翻訳を検査し、`(glossary-fix)` で違反分を
    再翻訳します
  * 一致判定は、英語側が単語境界つき、CJK 側が部分一致です
* **`glossaries/glossary-ja.lisp`** — Zork I の用語 59 語（場所・物品・登場人物）。
  言語の切り替え時に自動で読み込まれます。
* **データファイル名の指定**（`settings.lisp`）— 翻訳キャッシュと用語集の
  ファイル名を、言語コードから決めるのではなく起動スクリプトで明示的に指定できます。
  ゲームごと・プレイスルーごとにキャッシュを分けられます。
  * `(set-translation-file "zork2-ja.lisp")` と `(set-glossary-file ...)` を
    `(set-language ...)` より前に呼びます
  * キャッシュは `translations/`、用語集は `glossaries/` の中に置かれるため、
    ファイル名だけで足ります。ディレクトリを含む名前を指定した場合はそのまま
    使われます
  * `NIL` を渡すと言語別の名前に戻ります
  * `(show-config)` で解決先を確認できます
  * 名前にディレクトリが含まれる場合、初回保存時に作成します
* **バックエンドの明示的な選択** — 各 setup 関数が `*translation-backend*` を
  `:ollama` `:deepl` `:claude` のいずれかに設定し、`(translation-stats)` と
  `(check-environment)` に表示されます。
* **共通の HTTP 層** — `http-post-json` と `http-get`。curl を使い、無ければ
  PowerShell にフォールバックします。リクエストとレスポンスはどちらも UTF-8 の
  テンポラリファイルを経由するため、コマンドライン長の制限と、Windows で JSON を
  引数として渡した際の文字化けの両方を回避できます。
* **`*claude-model*`** — Claude のモデルをハードコードではなく設定で指定できるように
  なりました。

### 変更

* すべての LLM バックエンドが共通のプロンプト生成（`build-translation-prompt`）を
  使うようになりました。従来の 1 行のプロンプトに代わり、明示的な翻訳ルールと
  用語集のセクションを含みます。
* `clean-llm-output` が、モデルの応答から `<think>` ブロック、`Translation:` のような
  先頭のラベル、前後を囲む引用符を取り除きます。
* Claude バックエンド: 既定モデルを `claude-3-haiku-20240307` から
  `claude-3-5-haiku-latest` に変更し、リクエストボディを `-d` のコマンドライン引数
  ではなくファイル経由で渡すようにしました。
* **翻訳キャッシュと用語集を、それぞれ 1 ファイルに統一しました。** 従来はどちらも
  同梱分（`translations/` または `glossaries/` の中）と作業ディレクトリの分の
  2 ファイルから読み込み、書き込むのは後者だけ、という構成でした。両方とも
  1 段目を廃止し、指定したファイル（既定は
  `translations/translations-<言語>.lisp` と `glossaries/glossary-<言語>.lisp`）が
  読み込み・書き込みの唯一のファイルになりました。ファイルが存在しない場合は、
  黙って空で始めるのではなく起動時に表示します。
* 翻訳キャッシュは原文でソートして書き出すので、同じ内容なら毎回同じファイルに
  なります。用語集は以前からソート済みです。
* DeepL も共通の HTTP 層を使い、JSON ボディで送るようになりました。従来の独自の
  curl / PowerShell 経路とフォームエンコードを廃止しています。原文中の改行と
  引用符がそのまま保たれます。
* `*deepl-url*` でエンドポイントを選べます。無料キーは既定のまま、Pro キーの場合は
  `https://api.deepl.com/v2/translate` を指定してください。
* `(auto-translate-all)` は Ollama バックエンドでは待機を入れません（レート制限が
  ないため）。
* `(set-language ...)` がその言語の用語集も読み込みます。
* `(translation-stats)` と `(check-environment)` が、使用中のバックエンド、Ollama の
  モデルと URL、用語集の登録数を表示します。

### 修正

* **DeepL 翻訳がまったく動作していませんでした。** API キーを `auth_key` という
  リクエストパラメータで送っていましたが、DeepL はこの方式を廃止しており、すべての
  リクエストが `403 Missing Authorization header, expected
  'Authorization: DeepL-Auth-Key <API key>'` で拒否されていました。拒否された
  リクエストは課金対象外のため、DeepL の利用状況が 0 文字のままで、翻訳キャッシュも
  一度も書き出されませんでした。キーを `Authorization` ヘッダで送るようにしました。
  ([#1](https://github.com/fukuyori/zmachine-multilingual/issues/1))
* DeepL の失敗が完全に無言でした。拒否は Lisp のエラーにならないため握りつぶされ、
  その行が未翻訳のまま残るだけでした。エラー応答の `message` フィールドを表示し、
  `(test-deepl-api)` が失敗理由を報告するようにしました。
* Claude の応答が最初の引用符で切れる問題を修正しました。JSON の text フィールドを
  次の `"` を単純に検索して取り出していたため、訳文中の `\"` で止まっていました。
  エスケープを解釈して JSON 文字列を読むようにしました。
* `json-escape` が復帰文字・タブなどの制御文字をエスケープしていなかったため、
  それらを含むテキストで不正な JSON が生成され、リクエストが失敗していました。
  すべての制御文字をエスケープするようにしました。

## [0.3.3] - 2025-12-13

これ以前のリリースはこの変更履歴の作成前のものです。詳細はコミット履歴を参照してください。

## [0.3.2] - 2025-12-13
## [0.3.1] - 2025-12-11
## [0.3] - 2025-12-11
## [0.2.3] - 2025-12-11
## [0.2.2] - 2025-12-11
## [0.2.1] - 2025-12-11
## [0.2] - 2025-12-11
## [0.1] - 2025-12-11
